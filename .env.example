# Copy this file to ".env" and fill in real credentials (do NOT commit your .env).
# Put the real password into your local .env or into your environment/secret store.
#
# To set the new mailer password locally (example, DO NOT commit):
#   Create a file named .env in the same folder and add:
#     EMAIL_PASS=your_real_password_here
#
# Or set an environment variable for your session/host instead.

EMAIL_USER=tjsasphaltandconcreterepair@gmail.com
EMAIL_PASS=your_email_password_here
RECEIVER_EMAIL=${EMAIL_USER}
PORT=3000
